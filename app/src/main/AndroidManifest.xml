<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lesw.tree_knowledge">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <!-- <activity -->
        <!-- android:name=".MainActivity" -->
        <!-- android:theme="@style/AppTheme"> -->
        <!-- &lt;!&ndash;<intent-filter>&ndash;&gt; -->
        <!-- &lt;!&ndash;<action android:name="android.intent.action.MAIN" />&ndash;&gt; -->
        <!-- &lt;!&ndash;<category android:name="android.intent.category.LAUNCHER" />&ndash;&gt; -->
        <!-- &lt;!&ndash;</intent-filter>&ndash;&gt; -->
        <!-- </activity> -->
        <activity
            android:name="com.lesw.tree_knowledge.view.LoginActivity"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustPan|adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.lesw.tree_knowledge.view.SignupActivity"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustPan|adjustResize" />
        <activity
            android:name="com.lesw.tree_knowledge.view.MenuActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name="com.lesw.tree_knowledge.view.EmployeeListActivity"
            android:theme="@style/AppTheme" />
        <activity android:name="com.lesw.tree_knowledge.view.EmployeeActivity" />
        <activity android:name="com.lesw.tree_knowledge.view.ApprovalActivity"/>
    </application>
</manifest>